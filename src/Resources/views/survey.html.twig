<div class="{{ class }}">
    <h2 class="headline">
        {%- block headline %}
            {{- survey.title -}}
        {% endblock -%}
    </h2>
    <div class="wrapper">
        {% block content %}{% endblock %}
    </div>
</div>